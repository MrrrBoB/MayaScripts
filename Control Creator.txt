string $theJoint[] = `ls -sl`;
string $myControl[] = `circle -r 10`;
string $jointName = $theJoint[0];
select $myControl;
string $renamedCtrl = `rename ($jointName+"_Ctrl")`;
string $controlName = $myControl[0];
string $controlGroup[] = `doGroup 0 1 1`;
rename $controlGroup ($theJoint[0] + "_Ctrl_Grp");
select -add $theJoint;
performMatchTransform 0;
select -clear;
select -r $renamedCtrl;
xform -r -ro 0 90 0;
makeIdentity -apply true;
select -add $theJoint;
parentConstraint -mo -weight 1;
scaleConstraint -offset 1 1 1 -weight 1;